[project]
name = "t80-browse-probe"
version = "1.0.0"
description = "VM probe for #80 - does BrowseDlg's OK set the folder"
requires-python = ">=3.14"
dependencies = ["loguru>=0.7.3"]

[tool.uv]
package = false
