[project]
name = "t5t7-cleanup-probe"
version = "1.0.0"
description = "Runtime probe for todo-testme.md T5 and T7 - what <remove-on-uninstall> deletes, and what it leaves alone"
requires-python = ">=3.14"
dependencies = ["loguru>=0.7.3"]

[tool.uv]
package = false
