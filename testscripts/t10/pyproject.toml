[project]
name = "t10-preserve-probe"
version = "1.0.0"
description = "T10 - what a silent package does with preserve=yes registry values"
requires-python = ">=3.14"
dependencies = ["loguru>=0.7.3"]

[tool.uv]
package = false
