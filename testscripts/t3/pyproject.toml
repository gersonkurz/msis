[project]
name = "t3-x86-bundle-probe"
version = "1.0.0"
description = "Runtime probe for todo-testme.md T3 / issue #8 - does an x86 auto-bundle install the x86 VC++ runtime on 64-bit Windows"
requires-python = ">=3.14"
dependencies = [
    "loguru>=0.7.3",
]

[tool.uv]
package = false
