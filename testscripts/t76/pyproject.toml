[project]
name = "t76-upgrade-probe"
version = "1.0.0"
description = "VM probe for #76 - does the first upgrade from an older build still delete INSTALLDIR"
requires-python = ">=3.14"
dependencies = ["loguru>=0.7.3"]

[tool.uv]
package = false
