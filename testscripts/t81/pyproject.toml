[project]
name = "t81-guid-probe"
version = "1.0.0"
description = "VM probe for #81 - does the one-time component GUID change upgrade cleanly"
requires-python = ">=3.14"
dependencies = ["loguru>=0.7.3"]

[tool.uv]
package = false
