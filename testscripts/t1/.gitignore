# Everything here is regenerated by t1_preserve_probe.py on every run.
# The project itself is pyproject.toml, uv.lock, README.md and t1_preserve_probe.py.
rtprobe.reg
rtprobe.msis
rtprobe.wxs
readme.txt
*.log
.venv/
